# SCRIPT_DIR="$( cd -- "$( dirname -- "${BASH_SOURCE[0]:-$0}"; )" &> /dev/null && pwd 2> /dev/null; )";
# read -p 'Do you want to? (y/n)' RESP

# if [ $RESP = "y" ] || [ $RESP = "Y" ]
# then
#     echo "Yes"

# elif [ $RESP = "n" ] || [ $RESP = "N" ]
# then

#     echo "No"

# else
#     echo "Unrecognized response, looking for 'y' or 'n'"
# fi
exec zsh
gst